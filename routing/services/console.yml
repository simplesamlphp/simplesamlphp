---

services:
  # default configuration for services in *this* file
  _defaults:
    autowire: true
    autoconfigure: true

  SimpleSAML\Command\SspCacheClearCommand:
    tags: ['console.command']
    arguments:
      $cacheClearer: '@cache_clearer'
      $filesystem: '@filesystem'

  SimpleSAML\Command\AssetsPublishCommand:
    tags: ['console.command']
